{
  "tasks": [
    {
      "args": [
        "build",
        "-gcflags=all=-N -l",
        "-o",
        "${fileDirname}/__debug_bin"
      ],
      "command": "go",
      "label": "go: build (debug)",
      "options": {
        "cwd": "${fileDirname}"
      },
      "type": "shell"
    }
  ],
  "version": "2.0.0"
}
